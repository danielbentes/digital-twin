# Examples

- `hook-config.json` — Sample `PostToolUse` hook entry for the pushback detector. Not auto-installed.
- `sample-PROFILE.md` — An anonymized profile output (placeholder until a clean sample is generated).
- `sample-PROFILE.html` — Anonymized HTML version (placeholder).

To generate your own profile, run `/digital-twin:init` from any Claude Code session after installing the plugin.
